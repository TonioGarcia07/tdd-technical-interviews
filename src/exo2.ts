/*
 * Exo 2: common elements between two arrays
 *
 * ************************************************************************
 *
 * Given two arrays, find the elements that are in both.
 * Return the list of elements that are in both arrays.
 *
 * ************************************************************************
 */

const findCommonElements = (arrayA: string[], arrayB: string[]): string[] => {
  return [];
};

export default findCommonElements;
